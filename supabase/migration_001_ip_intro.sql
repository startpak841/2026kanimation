-- ============================================================================
-- Migration 001: IP 테이블에 영문 소개(intro_en) 컬럼 추가
-- ============================================================================
-- 실행 방법:
-- 1. Supabase 대시보드 → SQL Editor → New query
-- 2. 아래 내용 복사 → 붙여넣기 → Run
-- ============================================================================

ALTER TABLE ips
  ADD COLUMN IF NOT EXISTS intro_en TEXT;

-- ips 테이블이 이미 realtime에 추가되어 있다면 이 컬럼도 자동 포함됨.
-- 새로 시작하는 프로젝트는 schema.sql v1.1 이후로는 포함되어 있음.

-- 기존 participants.intro_en (회사 소개)과는 별개 컬럼입니다.
-- participants.intro_en = 회사 전체 소개 (1000자)
-- ips.intro_en          = 개별 IP 소개 (400자)
